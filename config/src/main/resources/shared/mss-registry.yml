eureka:
  instance:
    prefer-ip-address: true
  client:
    registerWithEureka: false
    fetchRegistry: false
    server:
      maxThreadsForPeerReplication: 0 #不发送对等数据，生产环境去掉此配置
      waitTimeInMsWhenSyncEmpty: 0
server:
  port: 8761